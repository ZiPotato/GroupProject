@page "/"
@using LähetysSeurantaConsole.View; 
@implements IView;
<PageTitle>Home</PageTitle>

<input type="text" id="ID"/>
<!-- basic test loop -->
@foreach (var parcel in parcelList)
{
	<ParcelWidget Parcel="parcel" />
	<h1></h1> <!-- temp? seperator -->
}


@code {
    IView view;
    string IView.Id { get; }
    public LähetysSeurantaConsole.Presenter.PackageIDHandling
    event EventHandler IView.AddPackage
    {
        add
        {
        }

        remove
        {
            throw new NotImplementedException();
        }
    }
    event EventHandler IView.DisplayLatestPackage
    {
        add
        {
            throw new NotImplementedException();
        }

        remove
        {
            throw new NotImplementedException();
        }
    }
    




    // replace this if we don't want to have the parcel list here
	private List<Parcel> parcelList = new() { null, null }; // test null values
}