@page "/"
@using LähetysSeurantaConsole.View;
@using LähetysSeurantaConsole.Presenter;
<PageTitle>Home</PageTitle>

<input class="ourtxt" type="text" @bind-value="ID"/>

<button class="ourbtn" onclick="@Click()">Add a package</button>
<!-- basic test loop -->
@foreach (var parcel in parcelList)
{
	<ParcelWidget Parcel="parcel" />
	<h1></h1> <!-- temp? seperator -->
}


@code {
	private string ID { get; set; }
//	private PackageIDHandling _package = new();

	public async Task<Parcel> Click()
	{
//		if (!string.IsNullOrEmpty(ID)) return await _package.AddPackage(ID);
		return null;
	}
	// replace this if we don't want to have the parcel list here
	private List<Parcel> parcelList = new() { null, null }; // test null values
}