@page "/"

<PageTitle>Home</PageTitle>

<!-- basic test loop -->
@foreach (var parcel in parcelList)
{
	<ParcelWidget Parcel="parcel" />
}


@code {
	private List<Parcel> parcelList = new() { null, null }; // replace this if we don't want to have the parcel list here
}